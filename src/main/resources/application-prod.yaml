spring:
  application:
    name: e-commerce-prod

  datasource:
    url: ${DB_URL_PROD}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jdbc:
    template:
      fetch-size: 200
